{% set name = "pytest-arraydiff" %}
{% set version = "0.5.0" %}
{% set git_url = "https://github.com/astrofrog/pytest-arraydiff" %}
{% set sha256 = "714149beffd0dfa085477c65791c1139b619602b049536353ce1a91397fb3bd2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . --no-deps -vv

requirements:
  host:
    - python
    - pip
    - pytest
    - numpy
    - six
  run:
    - python
    - pytest
    - numpy
    - six

test:
  imports:
    - pytest_arraydiff

about:
  home: {{ git_url }}
  license: BSD
  license_file: LICENSE
  summary: pytest plugin to help with comparing array output from tests
  description: |
    This is a py.test plugin to facilitate the generation and comparison of
    data arrays produced during tests.
  doc_url: {{ git_url }}
  dev_url: {{ git_url }}

extra:
  recipe-maintainers:
    - astrofrog-conda-forge
    - drdavella
    - mwcraig
